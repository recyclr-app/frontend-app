import { SafeAreaView, Text } from "react-native";
import React from "react";

const Settings = () => {
  return (
    <SafeAreaView>
      <Text>Sign out</Text>
    </SafeAreaView>
  );
};

export default Settings;
